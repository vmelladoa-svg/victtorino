// Web propia (WooCommerce tradeglobalchile.cl) — CONECTADO (solo lectura).
// La implementación real vive en server/web-feed.mjs (GET /wc/v3/orders, ventas por despachar)
// y se sirve junto al resto en GET /feed. El frontend hace un solo poll a /feed.
// Preguntas web (formulario de contacto) quedan pendientes: canal débil, se evaluará después.
